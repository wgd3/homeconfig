Each host in inventory has a vault-encrypted YAML file in this directory which defines a set of vars:

- `ansible_host`  the static IP address for that host
- `motd_avatar` (optional) if added, it will be used in the header for the MOTD
