.DS_Store

# no need to check virtualenv into VCS
venv/*

# hide sensitive ansible files
infra/password_file

# hide the group and host var files
infra/group_vars/**/*.yml
infra/host_vars/**/*.yml

# hide non-sample files in roles
infra/roles/**/files/*

# make sure we keep READMEs, samples, and gitkeep files
!infra/**/.gitkeep
!infra/**/README
!infra/**/*.sample
!infra/**/*.sh
